PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=medtrack
DB_PASSWORD=medtrack
DB_NAME=medtrack

JWT_SECRET=change-me
JWT_EXPIRES_IN=1h

# Needed for GET /v1/pharmacies/nearby — a Google Cloud API key with
# "Places API" enabled and billing set up. Without it that endpoint
# returns a 503 explaining what's missing; nothing else in the app needs it.
GOOGLE_MAPS_API_KEY=
