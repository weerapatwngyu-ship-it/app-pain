# app-pain

**MedTrack** — mobile app for medication dispensing reminders and patient
symptom tracking.

The Flutter source lives in [`medtrack/`](medtrack/), structured as a
feature-first Clean Architecture app (`auth`, `medication`,
`symptom_tracking`, `alerts`, `dashboard`). See
[`medtrack/README.md`](medtrack/README.md) for setup instructions.
