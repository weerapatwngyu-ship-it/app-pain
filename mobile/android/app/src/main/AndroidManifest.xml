<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Timed medication reminders. POST_NOTIFICATIONS is required from
         Android 13, and without an exact-alarm permission the system is free
         to defer a reminder by minutes or more, which defeats the point of a
         dose schedule. SCHEDULE_EXACT_ALARM is the one the user can grant;
         USE_EXACT_ALARM covers Android 14+, where an alarm clock style app
         may hold it outright. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- The reminder service. Scheduling a reminder and waiting to be woken
         did not work on this device — alarms stayed registered and unfired,
         because the app's process had been killed and a dead process cannot
         be woken. A foreground service is the supported way to ask to stay
         running, so the app watches the clock itself instead of asking. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <application
        android:label="MediGo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <service
            android:name=".ReminderService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Medication reminders must ring at an exact time. Scheduled alarms are not delivered on this class of device because the app process is killed first, so the app stays resident to watch the clock itself."/>
        </service>

        <!-- Rings whatever is due when the exact alarm arrives — the path that
             still works while the device is in deep sleep and the service's
             own timer is frozen. -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="false"/>

        <!-- The "กินแล้ว" and "เลื่อน 10 นาที" buttons on a dose reminder.
             Not exported: only our own notification triggers it. -->
        <receiver
            android:name=".DoseActionReceiver"
            android:exported="false"/>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required so url_launcher can open the "เปิดใน Google Maps" link
             (Google Maps app if installed, else a browser) — see
             https://pub.dev/packages/url_launcher#android-setup -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <!-- So the clock app is visible to us on Android 11+, for handing a
             reminder over as a real alarm. -->
        <intent>
            <action android:name="android.intent.action.SET_ALARM"/>
        </intent>
    </queries>
</manifest>
